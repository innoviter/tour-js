**Step One:** Installing  dependency
```
npm install
```

**Step Two:** Compile the assets. 
```
npm run watch
```

**Step Three**: Serve the Application via Symfony local web server. Use your own server to serve index.html
``` 
symfony serve -d
```